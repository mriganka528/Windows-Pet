/** Contact point in the sprite's 100 × 100 model. The raised paw reaches up to
 * the close button while most of the body stays over the message itself. */
export const NOTIFICATION_PAW = { x: 82, y: 30 }
export const SWAT_DURATION = 0.5
